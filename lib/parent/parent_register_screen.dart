import 'package:flutter/material.dart';

class parent_register_screen extends StatelessWidget {
  const parent_register_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}